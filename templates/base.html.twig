<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/pandicon.ico') }}">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=VT323&display=swap" rel="stylesheet">
        {# <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>"> #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <header class="site-header">
            <span class="site-title">My <del>Symfony</del> MVC Site</span>
        </header>
        <nav class="{% block navbar_class %}navbar{% endblock %}">
            <ul>
                <li><a href="{{ path('home') }}">Home</a></li>
                <li><a href="{{ path('about') }}">About</a></li>
                <li><div class="dropdown">
                    <a href="{{ path('report') }}">Report</a>
                    <div class="droplinks">
                        <a href="{{ path('report') }}#kmom01">Kmom01</a>
                    </div>
                </div></li>
                <li><a href="{{ path('api') }}">API</a></li>
                <li><a href="{{ path('lucky') }}">Lucky</a></li>
            </ul>
        </nav>
        {% block body %}{% endblock %}
        <footer class="{% block footer_class %}footer{% endblock %} columns">
            <div class="twothirds">In the words of Rushadicus - "neverborn rainbow beasts of bounty bounding towards and away from all which were fun and glunty upon the wake of morrow's mirth
                not to be claimed ordained or speculous, my species is thnarkus jicktoclade
                and its numbers go on for awhile and become slides are numbers become vegetable?
                swift math world imprinting upon itself the various longings invented by who and what for why where when?
                i am not that which says it is not baffled by the lack of non infinite explanations
                ithumixx bjickwilox"</div>
            <div class="onethird">
                <img class="downright" src="{{ asset('build/images/footerpossum.jpg') }}" height="250" alt="bitey possum">

            </div>
        </footer>
    </body>
</html>
